<table class="profile" summary="Profile information">
	<tr>
		<td class="main">
			<h1>
				<%= gravatar_for @user %>
				<%= @user.name %>
				<%= render 'follow_form' if signed_in? %>
			</h1>
			<%= render 'attend_logic' %>
			<%= render 'follow_logic' %>
		</td>
		<%= render 'sidebar' %>
	</tr>
</table>